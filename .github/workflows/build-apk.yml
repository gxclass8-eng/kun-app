name: Build KUN APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build APK via Online Tool
        run: |
          echo "✅ استخدم هذا الرابط لتحويل التطبيق إلى APK:"
          echo "https://gxclass8-eng.github.io/kun-app/kun_app.html"
          echo ""
          echo "اذهب إلى: https://www.pwabuilder.com"
          echo "الصق الرابط أعلاه"
          echo "اختر Android"
          echo "حمل الـ APK"
